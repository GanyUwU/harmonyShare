import 'package:flutter/material.dart';

class NgoDesc extends StatefulWidget {
  const NgoDesc({super.key});

  @override
  State<NgoDesc> createState() => _NgoDescState();
}

class _NgoDescState extends State<NgoDesc> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
